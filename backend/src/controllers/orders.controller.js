export const getOrdersFromUser = (req, res) => {
    //TODO
    //const {userId, sessionId} = res.locals.userInfo
};

export const postUserOrder = (req, res) => {
    //TODO
    //const {userId, sessionId} = res.locals.userInfo
};
